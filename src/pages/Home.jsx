import BentoGrid from '../components/BentoGrid';

const Home = () => {
    return (
        <div className="container" style={{ paddingTop: '6rem' }}>
            <BentoGrid />
        </div>
    );
};

export default Home;
